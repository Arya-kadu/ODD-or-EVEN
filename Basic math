/*PRN-2503033111378UD002
  Arya kadu 
  VLSI DESIGN */


#include <stdio.h>

void add(int a, int b)
{
    printf("Addition = %d\n", a + b);
}

int main()
{
    int x, y;

    printf("Enter two numbers: ");
    scanf("%d%d", &x, &y);

    add(x, y);

    return 0;
}
